import 'package:flutter/material.dart';

import 'unity_avatar_panel.dart';

/// Embedded Unity avatar on the home screen.
class HomeAvatarPanel extends StatelessWidget {
  const HomeAvatarPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const UnityAvatarPanel();
  }
}
